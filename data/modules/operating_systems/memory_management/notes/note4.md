### Page Tables

Paging divides logical memory into pages and physical memory into
frames.

A page table is used to map pages to frames.

Each process maintains its own page table.

Paging eliminates the need for contiguous memory allocation.
