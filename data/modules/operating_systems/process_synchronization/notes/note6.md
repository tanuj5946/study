### Monitor

A monitor is a high-level synchronization construct.

It encapsulates shared data and operations on that data.

Only one process can be active inside the monitor at a time.

Monitors simplify synchronization by avoiding explicit locking.
