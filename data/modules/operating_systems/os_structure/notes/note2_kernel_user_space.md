### Kernel and User Space

User Mode:
Applications run in user mode with limited privileges.

Kernel Mode:
OS core functions run in kernel mode with full privileges.

Mode Switching:
Occurs when a system call or interrupt transfers control
from user mode to kernel mode.

System Calls:
Interface between user applications and OS services.
Examples: fork(), exec(), read(), write().
