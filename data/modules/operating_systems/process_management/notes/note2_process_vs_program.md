### Process vs Program

A program is a static set of instructions stored on disk.
A process is a dynamic instance of a program in execution.

Key differences:
- Program is passive, process is active
- Multiple processes can be created from the same program
- Process has its own execution context
